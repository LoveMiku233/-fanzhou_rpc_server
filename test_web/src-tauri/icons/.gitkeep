# 应用图标目录

此目录用于存放应用图标文件。

## 需要的图标文件

根据不同平台需要以下格式的图标：

- `icon.ico` - Windows 图标
- `icon.png` - 通用 PNG 图标 (256x256 推荐)
- `icon.icns` - macOS 图标

## 生成图标

可以使用在线工具或命令行工具生成图标：

1. 准备一个 1024x1024 的 PNG 图片
2. 使用 `cargo tauri icon` 命令生成所有需要的格式
